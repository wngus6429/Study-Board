# Study-Board 공부용 보완 가이드

> **작성일**: 2026년 2월 20일  
> **목적**: 현재 구조에서 공부 관점으로 추가/보완할 포인트 정리

---

## 📋 현재 프로젝트 구조 요약

- **프론트엔드**: Next.js 14, Material-UI, TanStack React Query, Zustand
- **백엔드**: NestJS 10, TypeORM 0.3.20, MySQL 8.0+
- **실시간 통신**: Socket.IO 4.8.1
- **인증**: NextAuth.js (JWT + Session)
- **기능 완성도**: 약 95% (핵심 기능 전부 구현 완료)

---

## 🔴 우선순위 높음 (핵심 실무 패턴)

### 1. 단위 테스트 & E2E 테스트

현재 `.spec.ts` 파일이 존재하지만 내용이 거의 비어있음.

- **백엔드**: `story.service.spec.ts`, `story.controller.spec.ts` — Jest로 실제 테스트 작성
- **프론트엔드**: React Testing Library로 컴포넌트 테스트
- **E2E**: Playwright나 Cypress로 회원가입 → 로그인 → 글쓰기 플로우 테스트

> 💡 실무에서 테스트 코드는 거의 필수. 현재 완전히 비어있어서 공부 가치 매우 높음

---

### 2. TypeORM 마이그레이션 실습

`migrations` 폴더가 있지만 비어있음.

- 컬럼 추가/변경 마이그레이션 직접 작성해보기
- `typeorm migration:generate` → `migration:run` 흐름 익히기
- 롤백(`migration:revert`) 실습

> 💡 DB 스키마 변경이 실무에서 얼마나 중요한지 체감 가능

---

### 3. NestJS Guards / Interceptors / Pipes 심화

현재 `pipes` 폴더가 비어있고, 인증 로직이 서비스에 섞여 있음.

- Custom Guard 직접 구현 (`@UseGuards` 활용)
- Logging Interceptor, Response Transform Interceptor 추가
- Global Validation Pipe 세밀하게 설정

---

## 🟡 중간 우선순위 (심화 학습)

### 4. Redis 캐싱 레이어 추가

현재 React Query 클라이언트 캐싱만 있고, **서버 사이드 캐시가 없음**.

- NestJS `@nestjs/cache-manager` + Redis 연동
- 조회수 많은 게시글 목록 캐싱
- 세션/토큰 블랙리스트 관리

> 💡 분산 환경에서 필수적인 패턴

---

### 5. Next.js Server Components / Server Actions 전환

현재 대부분 `"use client"` + API 호출 구조.

- 일부 페이지를 **Server Component**로 전환하여 비교
- **Server Actions**으로 폼 제출 처리 실험
- `prefetchQuery` + `dehydrate` 패턴 확장 (ToDo.md에도 언급됨)

---

### 6. API Rate Limiting 고도화

현재 기본 Rate Limiting이 있다고 표시됐는데, 실제 구현 확인 필요.

- IP별 제한, 유저별 제한 분리
- **Throttler Guard** NestJS 내장 모듈 활용
- 슬라이딩 윈도우 vs 고정 윈도우 비교 실습

---

### 7. 환경별 설정 분리 (Config Module)

- `@nestjs/config` 제대로 활용하여 development/production 분리
- `.env.development`, `.env.production` 분리 관리
- 설정 값의 타입 안전성 보장 (`ConfigService` 제네릭 활용)

---

## 🟢 낮은 우선순위 (부가 학습)

### 8. WebSocket 심화 — 실시간 입력 표시

현재 채널 채팅은 있는데:

- "A님이 입력 중..." 타이핑 인디케이터 추가
- Socket 방 관리 최적화 (네임스페이스/룸 패턴)
- 재연결 로직 (연결 끊겼을 때 자동 재연결)

---

### 9. OpenAPI/Swagger 문서화 강화

현재 일부 `@ApiProperty`가 있는데:

- 모든 DTO에 Swagger 데코레이터 완성
- API 응답 스키마 문서화
- Bearer Auth 헤더 설정

---

### 10. Docker 컨테이너화 실습

`AWS_DEPLOYMENT_GUIDE.md`가 있는데:

- `docker-compose.yml`로 프론트 + 백 + MySQL + Redis 한번에 띄우기
- 개발환경 재현성 확보

> 💡 면접에서 인프라 이해도 보여줄 수 있음

---

## 💡 현재 구조에서 바로 발견한 개선 포인트

| 항목 | 현황 | 개선 방향 |
|------|------|-----------|
| `story.service.ts` | **74,702바이트** (매우 큰 파일) | 도메인별 서비스 분리 리팩터링 |
| `ToDo.md` 항목 92 | "트랜잭션 해야함" → 완료됐는데 체크 안 됨 | 정리 필요 |
| `migrations/` 폴더 | 비어있음 | 마이그레이션 실습 |
| `.spec.ts` 파일들 | 내용 거의 없음 | 테스트 코드 작성 |
| `pipes/` 폴더 | 비어있음 | Custom Validation Pipe 작성 |

---

## 🎯 추천 학습 순서

```
1️⃣  테스트 코드 작성 (story.service.spec.ts부터)
    → 기존 코드를 다시 이해하면서 가장 많이 배움

2️⃣  마이그레이션 실습
    → 컬럼 하나 추가하고 롤백해보기

3️⃣  story.service.ts 리팩터링
    → 74KB짜리 하나를 쪼개는 연습

4️⃣  Redis 캐싱 추가
    → 백엔드 고급 패턴 이해

5️⃣  Server Components 전환 실험
    → Next.js 15 최신 패턴 익히기
```

---

## 📚 참고 자료

- [TypeORM Migration 공식 문서](https://typeorm.io/migrations)
- [NestJS Guards 공식 문서](https://docs.nestjs.com/guards)
- [NestJS Interceptors 공식 문서](https://docs.nestjs.com/interceptors)
- [NestJS CacheManager (Redis)](https://docs.nestjs.com/techniques/caching)
- [Next.js Server Actions 공식 문서](https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations)
- [Jest 공식 문서](https://jestjs.io/docs/getting-started)
- [class-validator GitHub](https://github.com/typestack/class-validator)

---

_마지막 업데이트: 2026년 2월 20일_
